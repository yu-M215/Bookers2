<% if user_signed_in? %>
  <div class="row justify-content-center pb-2">
    <%= form_with url: search_path, method: :get, local: true do |f| %>
      <%= f.search_field :content %>
      <%= f.select :model, [ ["User","user"],["Book","book"] ] %>
      <%= f.select :method, [ ["完全一致","perfect"],["部分一致","pertial"] ] %>
      <%= f.submit "検索" %>
    <% end %>
  </div>
<% end %>